apiVersion: v1
kind: Service
metadata:
  name: log-output-deployment-svc
spec:
  type: ClusterIP
  selector:
    app: log-output-deployment 
  ports:
    - port: 2345
      protocol: TCP
      targetPort: ${LOG_OUTPUT_PORT}