apiVersion: v1
kind: Service
metadata:
  name: ping-pong-stset-db-svc
  labels:
    app: ping-pong-stset-db
spec:
  ports:
  - port: 5432
    name: web
  clusterIP: None
  selector:
    app: ping-pong-stset