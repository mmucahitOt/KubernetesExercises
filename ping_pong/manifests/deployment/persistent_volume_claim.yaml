apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ping-pong-claim
  namespace: exercises
spec:
  storageClassName: my-persistent-volume-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi