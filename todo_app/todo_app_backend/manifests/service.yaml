apiVersion: v1
kind: Service
metadata:
  name: todo-app-backend-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: todo-app-stset 
  ports:
    - port: 2346
      protocol: TCP
      targetPort: ${TODO_APP_BACKEND_PORT}