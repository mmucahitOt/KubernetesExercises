apiVersion: v1
kind: Service
metadata:
  name: todo-app-backend-deployment-svc
spec:
  type: ClusterIP
  selector:
    app: todo-app-deployment 
  ports:
    - port: 2346
      protocol: TCP
      targetPort: ${TODO_APP_BACKEND_PORT}