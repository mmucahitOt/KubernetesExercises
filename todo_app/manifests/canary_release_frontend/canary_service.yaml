apiVersion: v1
kind: Service
metadata:
  name: todo-app-frontend-canary-svc
  namespace: project
spec:
  type: ClusterIP
  selector:
    app: todo-app-frontend-deployment
  ports:
    - port: 2346
      protocol: TCP
      targetPort: ${TODO_APP_PORT}