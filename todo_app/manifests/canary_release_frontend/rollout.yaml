apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: todo-app-frontend-rollout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app-frontend-deployment
  strategy:
    canary:
      stableService: todo-app-frontend-deployment-svc 
      canaryService: todo-app-frontend-canary-svc
      steps:
      - setWeight: 25
      - pause:
          duration: 30s
      - setWeight: 50
      - analysis:
          templates:
          - templateName: todo-app-frontend-restart-rate
      - pause:
          duration: 30s
  template:
    metadata:
      labels:
        app: todo-app-frontend-deployment
    spec:
      containers:
        - name: todo-app-container
          image: ${DOCKER_REGISTRY}/todo_app:${IMAGE_VERSION}
          ports:
            - name: http
              containerPort: ${TODO_APP_PORT}
          env:
            - name: TODO_APP_PORT
              value: "${TODO_APP_PORT}"
            - name: PING_PONG_DB_URL
              value: "${PING_PONG_DB_URL}"
          readinessProbe:
            initialDelaySeconds: 10
            periodSeconds: 5
            httpGet:
              path: /healthz
              port: ${TODO_APP_PORT}
          livenessProbe:
            initialDelaySeconds: 20
            periodSeconds: 5
            httpGet:
              path: /healthz
              port: ${TODO_APP_PORT}
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"