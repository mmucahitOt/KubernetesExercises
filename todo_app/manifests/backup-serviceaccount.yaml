apiVersion: v1
kind: ServiceAccount
metadata:
  name: todo-backup-sa
  namespace: project
  # For Workload Identity (if enabled), uncomment and add:
  # annotations:
  #   iam.gke.io/gcp-service-account: todo-backup-sa@PROJECT_ID.iam.gserviceaccount.com

